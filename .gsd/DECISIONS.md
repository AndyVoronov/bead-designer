# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | 3D framework | React Three Fiber + Three.js | Декларативный React-рендерер для 3D, лучшая интеграция с React-экосистемой, built-in event system для интерактивности, PerformanceMonitor для мобильной адаптации | Yes — если производительность окажется недостаточной |
| D002 | M001 | arch | Physics engine | React Three Rapier (Rapier WASM) | Rope joints, spring joints, spherical joints — всё что нужно для цепочки бусин. WASM = быстрая физика в браузере. Подтверждено документацией. | No |
| D003 | M001 | arch | Web framework | Next.js (App Router) | SSR, API routes для бэкенда, React-экосистема, хорошая история деплоя на VPS | No |
| D004 | M001 | arch | Database | PostgreSQL + Prisma ORM | Надёжная реляционная БД для продуктовых данных. Prisma — type-safe ORM, миграции, seed data | Yes — если reg.ru VPS не тянет PostgreSQL |
| D005 | M001 | arch | State management | Zustand (3D state) + React Query (server state) | Zustand — легковесный, отлично для 3D-сцены. React Query — кэширование и синхронизация серверных данных | Yes — если growing pains |
| D006 | M001 | pattern | Rendering pipeline | AdaptiveRenderer с PerformanceMonitor + dynamic DPR | Автоматическое снижение качества при падении FPS на мобильных. Post-processing (SSGI, HBAO) отключается на слабых устройствах. | Yes — при реальном профилировании |
| D007 | M001 | pattern | Design serialization | JSON → LZ-String compression → base64url | Компактные URL для шеринга. LZ-String даёт 60-70% сжатие на типичных JSON-данных. base64url — URL-safe. | Yes — если URL длины окажется недостаточно |
| D008 | M001 | integration | Telegram notification | Deep link без бота (https://t.me/VoronovAndrey?text=...) | Простота: один URL redirect, не нужен Telegram Bot API token. Предзаполненное сообщение с кодом изделия. | Yes — если понадобится bot API для richer interaction |
| D009 | M001 | arch | Styling | Tailwind CSS, mobile-first | Быстрая разработка responsive UI. Utility-first отлично для мобильного-first подхода. | No |
| D010 | M001 | arch | Deployment | Node.js на VPS reg.ru + Nginx reverse proxy + Let's Encrypt | Стандартный стек для Next.js на VPS. Docker опционально. | Yes — если reg.ru предоставляет иной hosting stack |
| D011 | M001 | ux | Order flow | Минимальная трения: один тап «Заказать» → БД → Telegram | Никаких форм, полей, регистрации. Максимальная конверсия. | Yes — если нужен будет сбор данных о клиенте |
| D012 | M001 | ux | Mobile-first | Мобильный браузер — primary target, десктоп — secondary | «Основная масса будет с телефона». Адаптивный рендеринг, тач-жесты, оптимизация bundle size. | No |
| D013 | M001 | T01 | Turbopack config | turbopack:{} instead of webpack experiments | Next.js 16 defaults to Turbopack; webpack config causes build failure. Turbopack handles WASM natively. | Yes — if webpack needed for specific loader |
| D014 | M001 | T01 | SSR boundary pattern | SceneLoader client wrapper for dynamic(ssr:false) | Next.js 16 forbids ssr:false in Server Components. Client wrapper isolates the boundary cleanly. | Yes — if Next.js re-allows it |
| D015 | M001 | T01 | Dev server from real path | Run npm run dev from C:\Users\Andy\.gsd\projects\... not junction | Windows junction causes Next.js to double-concatenate paths, resulting in ENOENT on routes-manifest.json. | No |
| D016 | M001 | T02 | MeshLine R3F type augmentation | Plain object type, not DetailedHTMLProps | R3F ThreeElements augmentation for meshLineMaterial must use plain object type (with ref/color/lineWidth etc.) instead of React.DetailedHTMLProps. HTML base types bring HTMLElement ref which conflicts with Three.js MeshLineMaterial ref. Include `attach` and `args` (ConstructorParameters) props. | Yes |
| D017 | M001 | S02 | R3F canvas touch-action override | CSS !important on .canvas-container canvas | R3F/Three.js sets inline `touch-action: auto` on the canvas element, which overrides the CSS cascade. Without `!important`, the browser intercepts touch events for scroll/zoom, breaking 3D interaction on mobile. | Yes — if R3F changes inline style behavior |
| D018 | M001 | S02 | Zustand access pattern for R3F | getState() in imperative callbacks, hook in React components | R3F pointer event callbacks (onPointerDown/Up) are imperative contexts where hook subscriptions cause stale closures. Using `useDragStore.getState()` avoids re-renders and closure issues. For React components needing reactive state (e.g., OrbitControls `enabled`), use hook subscription (`useDragStore(s => s.isDragging)`). | Yes — if React/R3F context model changes |
| D019 | M001 | S02 | Procedural bump textures | 16×16 canvas noise for knit/wood bump maps | No PNG textures available until S03/S06. Procedural CanvasTexture via `document.createElement('canvas')` provides visual material differentiation without external files. BeadMaterial component designed with future `textureUrl` prop for drei useTexture integration. | Yes — when real textures become available |
| D020 | M001 | S03 | Editor architecture | Zustand store (useDesignStore) as single source of truth; Scene stays "dumb" (props-based) | Catalog, toolbar, and 3D scene all read/write useDesignStore. Scene component receives beads/selectedBeadId as props — never subscribes to store directly. This keeps the 3D scene testable in isolation and avoids coupling rendering to state management. | Yes — if rendering becomes too complex for prop drilling |
| D021 | M001 | S03 | Catalog data storage | Static array for S03, dynamic API for S06 | 100 catalog beads as const array in src/data/catalogBeads.ts unblocks editor development without DB dependency. S06 will migrate to API-backed catalog with admin CRUD. Prisma schema (Bead model) written but no migrations run. | Yes — when S06 activates the database layer |
| D022 | M001 | S03 | Tap vs drag detection | Time threshold 200ms + distance threshold 0.05 NDC | Pointer events track startTime and startPos on pointerdown; on pointerup, if elapsed < 200ms AND distance < 0.05 NDC → tap (select bead), otherwise → drag (kinematic movement). Drag completion auto-deselects. | Yes — if real mobile touch response differs significantly |
| D023 | M001 | S03 | Touch isolation for catalog panel | stopPropagation + touch-action: pan-y + !important CSS | Global touch-action: none on html/body/canvas prevents browser gestures on 3D canvas. Catalog panel needs vertical scroll. Belt-and-suspenders: stopPropagation on panel touch events + touch-action: pan-y inline style + .catalog-scroll CSS class with !important. | Yes — if R3F changes inline style behavior or DOM structure shifts |
| D024 | M001 | T02 | Local database provider | SQLite via libsql adapter for local dev; PostgreSQL planned for production | User doesn't have Docker available locally. SQLite works out-of-the-box with zero external dependencies. Prisma 7 uses @prisma/adapter-libsql for SQLite connections. When deploying to reg.ru VPS, switch provider back to postgresql and update adapter to PrismaPg. | Yes — when deploying to production |
| D025 | M001 | T02 | Prisma 7 config | prisma.config.ts with dotenv for connection URL | Prisma 7 removed `url` from schema.prisma datasource. Connection URL must be in `prisma.config.ts` using `defineConfig()` + `dotenv/config` (since env() reads from process.env, not .env file). Client requires driver adapter (PrismaLibSql for SQLite, PrismaPg for PostgreSQL). Export name is `PrismaLibSql` (lowercase 'ql'), not `PrismaLibSQL`. | Yes — if Prisma changes config API |
